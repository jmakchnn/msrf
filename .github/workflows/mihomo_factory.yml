name: Mihomo Rule Factory

# 防止工作流冲突
concurrency:
  group: mihomo-factory-${{ github.ref }}
  cancel-in-progress: true

on:
  schedule:
    - cron: '30 0 * * *' # 每天 UTC 00:30 运行
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'src/domain/**'
      - 'src/ip/**'
      - '.github/workflows/mihomo_factory.yml'

permissions:
  contents: write

jobs:
  produce-mrs-rules:
    runs-on: ubuntu-latest
    # 防止卡死，30分钟超时
    timeout-minutes: 30

    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          # 重要：获取完整历史，避免推送冲突
          fetch-depth: 0
      
      - name: 转换规则文件
        run: |
          # 遇到错误立即停止
          set -e
          echo "🚀 开始处理 Mihomo 规则..."
          
          SOURCE_DIR="src"
          OUTPUT_DIR="rule-set"
          
          # 创建输出目录
          mkdir -p "$OUTPUT_DIR"
          
          # 使用 Docker 运行 Mihomo 转换
          DOCKER_CMD="docker run --rm -v $PWD:/workspace --user $(id -u):$(id -g) metacubex/mihomo:latest"
          
          # 处理域名规则
          echo "📁 处理域名规则..."
          for file in "$SOURCE_DIR"/domain/*.list "$SOURCE_DIR"/domain/*.txt; do
            [ -e "$file" ] || continue
            filename=$(basename "$file" .list)
            filename=$(basename "$filename" .txt)
            echo "🔄 转换: $filename"
            $DOCKER_CMD convert-ruleset domain text "/workspace/$file" "/workspace/$OUTPUT_DIR/$filename.mrs"
          done
          
          for file in "$SOURCE_DIR"/domain/*.yaml; do
            [ -e "$file" ] || continue
            filename=$(basename "$file" .yaml)
            echo "🔄 转换: $filename"
            $DOCKER_CMD convert-ruleset domain yaml "/workspace/$file" "/workspace/$OUTPUT_DIR/$filename.mrs"
          done
          
          # 处理 IP 规则
          echo "📁 处理IP规则..."
          for file in "$SOURCE_DIR"/ip/*.list "$SOURCE_DIR"/ip/*.txt; do
            [ -e "$file" ] || continue
            filename=$(basename "$file" .list)
            filename=$(basename "$filename" .txt)
            echo "🔄 转换: $filename"
            $DOCKER_CMD convert-ruleset ipcidr text "/workspace/$file" "/workspace/$OUTPUT_DIR/$filename.mrs"
          done
          
          for file in "$SOURCE_DIR"/ip/*.yaml; do
            [ -e "$file" ] || continue
            filename=$(basename "$file" .yaml)
            echo "🔄 转换: $filename"
            $DOCKER_CMD convert-ruleset ipcidr yaml "/workspace/$file" "/workspace/$OUTPUT_DIR/$filename.mrs"
          done
          
          echo "✅ 转换完成！生成的文件："
          ls -la "$OUTPUT_DIR/"

      - name: 提交更改
        # 重要：只有上一步成功且有文件变更时才提交
        if: success()
        run: |
          echo "📦 检查文件变更..."
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          
          # 检查是否有文件需要提交
          if git diff --quiet --exit-code -- rule-set/; then
            echo "✅ 没有文件变更，跳过提交"
            exit 0
          fi
          
          echo "📝 提交变更到仓库..."
          git add -f rule-set/*.mrs
          git commit -m "Mihomo: 更新规则集 [skip ci]"
          git push
          
          echo "🎉 提交成功！"
